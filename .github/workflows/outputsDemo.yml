name: Outputs Demo
on: push
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: step-1
        id: id-1-1-0
        run: echo "version=1.0.0" >> $GITHUB_OUTPUT
      - name: step-2
        run: echo "This version is ${{  steps.id-1-1-0.outputs.version}}"
        
